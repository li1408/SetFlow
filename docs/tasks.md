# SetFlow MVP 任务清单

状态：**执行中**

## A. 基线

- [x] 建立独立仓库。
- [x] 写产品规格、计划、任务与参考审计。
- [x] 用户确认规格与工具链安装范围。
- [x] 记录主手机型号：小米 15。
- [ ] 连接真机后读取实际 HyperOS 与 Android 版本。

## B. 工程骨架

- [ ] 写基础渲染与配置失败测试。
- [ ] 配置 React、TypeScript、Vite、Vitest、Testing Library。
- [ ] 配置 Capacitor、Dexie、Zod、GSAP、`@gsap/react`。
- [ ] 配置 lint、typecheck、test、build 脚本。
- [ ] 建立设计 token、Repository 与原生适配器接口。

验收：`npm test`、`npm run typecheck`、`npm run build` 通过。

## C. 领域与计划生成

- [ ] 为实体、不变量和状态转换写失败测试。
- [ ] 实现动作、计划、训练快照和组记录类型。
- [ ] 为规则生成器写确定性、器械、时长和计量类型测试。
- [ ] 实现徒手优先的原创动作库。
- [ ] 实现规则生成器与可编辑输出。

验收：同输入同输出；无器械不泄漏器械动作；每个动作有完整处方。

## D. 本地持久化

- [ ] 为 schema、迁移、事务回滚和恢复写失败测试。
- [ ] 实现 Dexie schema 与 Repository。
- [ ] 实现计划 CRUD、当前计划和今日安排。
- [ ] 实现不可变训练快照与历史。
- [ ] 实现完成组 + 活动计时器的原子事务。
- [ ] 实现数据库重开与升级保留测试。

验收：刷新、退出进程和升级后数据正确；编辑计划不改历史。

## E. 页面流程

- [ ] 首次进入与生成向导。
- [ ] 手动计划创建/编辑。
- [ ] 今日首页与开始/继续训练。
- [ ] 独立训练执行页。
- [ ] 休息状态与 `-15/+15/跳过`。
- [ ] 训练总结与历史列表。
- [ ] 设置与提醒能力状态。

验收：常见手机尺寸下完整离线跑通，无横向溢出。

## F. Android 提醒

- [ ] 安装隔离的完整 JDK/Android SDK。
- [ ] 添加 Android 平台并固定可复现环境脚本。
- [ ] 配置通知权限、精确提醒权限和频道。
- [ ] 实现通知调度、重排、取消和幂等恢复。
- [ ] 实现数据库计时器与系统待处理通知的启动/回前台协调器。
- [ ] 配置前台音效与 Haptics。
- [ ] 禁用云备份、远程导航和不必要网络权限。
- [ ] 真机验证后台、锁屏、熄屏、划走任务和权限拒绝。
- [ ] 小米 15 验证电池“无限制”、后台自启动与通知频道配置引导。

验收：满足规格第 7、11 节；系统抑制条件被清晰展示。

## G. GSAP 与体验

- [ ] 用 `useGSAP()` 和 scoped ref 实现组件动效。
- [ ] 用 timeline 实现完成组和训练/休息转场。
- [ ] 用 matchMedia 处理窄屏和 reduced-motion。
- [ ] 只动画 transform/opacity 等合成属性。
- [ ] 验证卸载/重挂载无残留 tween、listener 或计时器。
- [ ] 完成键盘、焦点、语义、状态播报、对比度检查。

验收：关闭动画仍可完成训练；控制台无 GSAP/React 警告。

## H. 交付

- [ ] 全量测试与生产构建。
- [ ] 浏览器控制台、网络、可访问性与截图复核。
- [ ] APK 安装、离线、冷启动、升级保留数据验证。
- [ ] 独立代码质量与安全复核。
- [ ] 输出 APK、SHA-256、安装命令和已知限制。

首个交付文件：`android\app\build\outputs\apk\debug\app-debug.apk`。
